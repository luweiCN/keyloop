{"text":"if cls.regex is not None:\n        strategy = st.from_regex(cls.regex)\n        if cls.strip_whitespace:\n            strategy = strategy.filter(lambda s: s == s.strip())\n    elif cls.strip_whitespace:\n        repeats = '{{{},{}}}'.format(\n            min_size - 2 if min_size > 2 else 0,\n            max_size - 2 if (max_size or 0) > 2 else '',\n        )\n        if min_size >= 2:\n            strategy = st.from_regex(rf'\\W.{repeats}\\W')\n        elif min_size == 1:\n            strategy = st.from_regex(rf'\\W(.{repeats}\\W)?')\n        else:\n            assert min_size == 0\n            strategy = st.from_regex(rf'(\\W(.{repeats}\\W)?)?')","source":"v4-fastapi-pydantic-pydantic-pydantic-v1-hypothesis-plugin-py-363-378-2621","language":"fastapi","syntax_language":"python","framework":"fastapi","project":"keyloop-corpus","level":"block","difficulty":"hard","score":11}
